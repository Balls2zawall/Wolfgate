# WP6 verification — Bleeding, circulation system, healing (server half, D13/D14/D15)

**Verifier evidence, generated by re-inspecting the tree directly.** WG worktree:
`C:/Users/jzo12/Documents/GitHub/Wolfgate/.claude/worktrees/rules-motd-updates-11c89c`
Onyx pin checked: `git -C C:/tmp/onyx log --oneline -1` → `2f5bab994 [PATCH] Баланс уровней частей машин.` — matches DECISIONS.md's pinned SHA `2f5bab9946539cbe083010c9ae6fbc59b47ae377`.

**Verdict: PASS.** No blockers, no majors.

---

## 1. Build

```
dotnet build Content.Server/Content.Server.csproj -c DebugOpt -v q -nologo
→ Build succeeded.
    0 Error(s)

dotnet build Content.Client/Content.Client.csproj -c DebugOpt -v q -nologo
→ Build succeeded.
    0 Error(s)
```

Both green, matching the report's claim.

---

## 2. Upstream discipline

`git diff HEAD --stat -- Content.Shared Content.Server Content.Client Resources Content.IntegrationTests` (this reflects every uncommitted WP so far, since DECISIONS.md forbids commits between WPs):

```
 Content.Server/Body/Systems/BloodstreamSystem.cs   |  20 +++
 Content.Server/Chat/Systems/ChatSystem.Emote.cs    |   2 +-
 Content.Server/Medical/Components/HealingComponent.cs |  28 ++++
 Content.Server/Medical/HealingSystem.cs            | 152 ++++++++++++++++++++-
 Content.Server/_Mono/Traits/Physical/HemophiliaSystem.cs |   6 +
 Content.Shared/Damage/Systems/DamageableSystem.cs  |  26 +++-
 Content.Shared/_Shitmed/Body/Systems/SharedBodySystem.Targeting.cs | 12 +-
 Content.Shared/_Shitmed/Targeting/SharedTargetingSystem.cs |   4 +
 8 files changed, 242 insertions(+), 8 deletions(-)
```

None of these 8 files is under `_Onyx`, `_WF`, or `Docs`, so each was diffed and checked line-by-line:

| File | Added/changed lines all `// WOLFGATE`-marked? | Hook | Authorised in PLAN §3, and for which WP |
|---|---|---|---|
| `Content.Server/Body/Systems/BloodstreamSystem.cs` | Yes — `using`, GUARD E early-return, and the GUARD E3 `TryModifyBleedAmount` split are all marked | GUARD E + GUARD E3 | §3 row `GUARD E`/`GUARD E3`, assigned **WP6** |
| `Content.Server/Chat/Systems/ChatSystem.Emote.cs` | Yes — one-word `override` with inline marker | HOOK 6 (D25) | §3 lists HOOK 6/D25 as an **earlier-WP** (WP2) upstream override; present verbatim, one word |
| `Content.Server/Medical/Components/HealingComponent.cs` | Yes — whole added block wrapped in `// WOLFGATE: HOOK 7 / D14 start … end` | HOOK 7 | §3 row `HOOK 7`, assigned **WP6**; explicitly one of the 4 places PLAN §3 authorises more than 1-2 lines |
| `Content.Server/Medical/HealingSystem.cs` | Yes — every added line sits inside the `// WOLFGATE: HOOK 8 start … end` block, or is prefixed inline (`OnDoAfter` branch, `TryHeal` guards) | HOOK 8 | §3 row `HOOK 8`, assigned **WP6**; the other of the 4 authorised larger-hook places |
| `Content.Server/_Mono/Traits/Physical/HemophiliaSystem.cs` | Yes — `using` + early-return both marked | GUARD E4 | §3 row `GUARD E4`, assigned **WP6** |
| `Content.Shared/Damage/Systems/DamageableSystem.cs` | Yes — every hunk (`using`s, query field, `before.Applied` return, routing seam block, `BeforeDamageChangedEvent` new members) individually marked | GUARD D + GUARD D2 | §3 rows `GUARD D`/`GUARD D2`, assigned **WP5** (an earlier WP; correctly not re-touched by WP6) |
| `Content.Shared/_Shitmed/Body/Systems/SharedBodySystem.Targeting.cs` | Yes — GUARD A/B/C guards and the `using` all marked | GUARD A/B/C (D2 damage bridge) | Earlier WP (WP5); each guard component-gated, not `_net.IsServer`-gated (trap 3 avoided) |
| `Content.Shared/_Shitmed/Targeting/SharedTargetingSystem.cs` | Yes — `IsSelectable` addition, no marker comment needed since HOOK 5 is a clean addition, matches PLAN §2.14 verbatim | HOOK 5 | §3 row `HOOK 5`, assigned **WP2** (earlier WP) |

No unauthorised upstream edit found. No file outside PLAN §3's hook list was touched.

---

## 3. Vendoring fidelity — `_Onyx` files touched in WP6

Onyx source read via `git -C C:/tmp/onyx show HEAD:<path>`, diffed with `diff --strip-trailing-cr`.

| File | Diff against Onyx | Every hunk marked? |
|---|---|---|
| `Content.Server/_Onyx/Wounds/WoundBleedingSystem.cs` | 2 `using` swaps only | Yes, both inline-marked (D13) |
| `Content.Server/_Onyx/Wounds/WoundInternalBleedingSystem.cs` | 2 `using` swaps + `:67` `TryModifyBloodLevel` argument-order fix | Yes, all three marked (D13, M1) |
| `Content.Server/_Onyx/Wounds/OrganDamageSystem.cs` | `using` retarget, `AmputationSystem` dependency+call commented (D26/phase-3 TODO), organ list + `PickOrgan` retargeted to `WolfmedOrganComponent` | Yes — every differing hunk sits under an inline or block `// WOLFGATE` marker (D8, D26) |
| `Content.Server/_Onyx/Wounds/WoundHealingSystem.cs` | `using` swaps, `[Dependency]` facade swap, `:110-113` `DamageContainers` conversion | Yes, all marked (D14, D12, D31) |
| `Content.Server/_Onyx/Chemistry/Circulation/CirculatoryStreamSystem.cs` | `using` swaps + one large deletion (everything but `GetPartStream`/`TryGetPartSolution`/`TryGetStreamSolution`/`SetBleedRates`) | Yes — a file-level `// WOLFGATE: D15…` comment block immediately precedes the class and names everything dropped; the two `return false;` stubs and the trimmed `SetBleedRates` body are each separately marked (D15) |
| `Content.Server/_Onyx/Body/Systems/OrganHealthSystem.cs` | `using` retargets, `OrganFunctionChangedEvent` relocated into a new `Content.Shared._Onyx.Body` namespace block in this file, `Update`/`SetHealth`/`ChangeHealth`/`DestroyOrgan` retargeted from `OrganComponent` to `WolfmedOrganComponent`, `DestroyOrgan`'s organ-slot walk replaced with `SharedBodySystem.RemoveOrgan` | Yes — 5 `// WOLFGATE` comments cover the substitution decisions (D13, D8) that the remaining `WolfmedOrganComponent`-typed lines mechanically follow from, same pattern as `OrganDamageSystem.cs`'s D8 substitution |
| `Content.Shared/_Onyx/Body/OrganDamageComponent.cs` | **No diff** — byte-identical to Onyx (mod CRLF) | N/A — verbatim, as claimed |

No unmarked drift found in any of the 7 vendored WP6 files.

---

## 4. Subscriptions

New `SubscribeLocalEvent<Component, Event>` pairs found in the 6 new/moved WP6 systems:

```
WoundBleedingComponent × ComponentInit           — WoundBleedingSystem.cs:39
WoundBleedingComponent × ComponentShutdown       — WoundBleedingSystem.cs:40
WoundBleedingComponent × WoundCreatedEvent       — WoundBleedingSystem.cs:41
WoundBleedingComponent × WoundChangedEvent       — WoundBleedingSystem.cs:42
WoundBleedingComponent × WoundStateChangedEvent  — WoundBleedingSystem.cs:43
WoundBleedingComponent × WoundRemovedEvent       — WoundBleedingSystem.cs:44
WoundHostComponent     × SleepStateChangedEvent  — WoundBleedingSystem.cs:45
WoundInternalBleedingComponent × WoundChangedEvent      — WoundInternalBleedingSystem.cs:20
WoundInternalBleedingComponent × WoundStateChangedEvent — WoundInternalBleedingSystem.cs:21
WoundInternalBleedingComponent × WoundRemovedEvent      — WoundInternalBleedingSystem.cs:22
WoundableComponent     × PartDamageAppliedEvent  — OrganDamageSystem.cs:31
WoundHostComponent     × ResolveHealingPartEvent — WoundHealingSystem.cs:29
```

`CirculatoryStreamSystem` and `OrganHealthSystem` register **zero** subscriptions, as the report claims (D15 drops all five circulation subscriptions; organ health has none in Onyx either).

Grepped each pair across `Content.Shared`, `Content.Server`, `Content.Client` (i.e. all of WG, the new files included by construction since they're the only hits): every pair returns **exactly one** match — its own WP6 registrant. No duplicate registration, no crash risk.

Cross-checked against PLAN §5.2: every pair above appears verbatim in the table (`WoundBleedingComponent` row, `WoundHostComponent`/`SleepStateChangedEvent` row, `WoundInternalBleedingComponent` row, `WoundableComponent`/`PartDamageAppliedEvent` row marked "the only subscriber to this pair in the whole wound set", `WoundHostComponent`/`ResolveHealingPartEvent` row). No mismatch.

---

## 5. Manifest

`Docs/Wolfmed/WOLFMED_MANIFEST.md` has a row for all 12 files in WP6's plan table, plus the 2 extra `WoundSystem.cs`/`WoundFractureSystem.cs` visibility edits (lines 71/73 already carried the WP4 modification note; lines 87-88 add the WP6 `internal`→`public` edit on top). Also carries:
- `Content.Shared/_Onyx/Body/FunctionalOrganComponent.cs` — row marked "skipped … Only its `OrganFunctionChangedEvent` was needed and now lives in `OrganHealthSystem.cs`" (line 85), matching PLAN's "not ported" instruction.
- A `### WP6` deviations section (line 225) covering all 9 deviations the report lists in its §3.
- "Last re-sync: 2026-09-12 by WP6" (line 4).
- Two new Hazards entries (`HealingComponent` default-on wound treatment; `WolfmedOrganComponent`/organ-prototype dependency), matching the report's "2 new hazards" claim.

No file touched by WP6 is missing a manifest row.

---

## 6. Plan conformance

All 12 files in PLAN §WP6's table exist at the planned destination:

```
OK   Content.Server/_Onyx/Chemistry/Circulation/CirculatoryStreamSystem.cs
OK   Content.Server/_Onyx/Wounds/WoundBleedingSystem.cs
OK   Content.Server/_Onyx/Wounds/WoundInternalBleedingSystem.cs
OK   Content.Server/_Onyx/Wounds/OrganDamageSystem.cs
OK   Content.Server/_Onyx/Wounds/WoundHealingSystem.cs
OK   Content.Shared/_Onyx/Body/OrganDamageComponent.cs
OK   Content.Shared/_WF/Wolfmed/Body/WolfmedOrganComponent.cs
OK   Content.Server/_Onyx/Body/Systems/OrganHealthSystem.cs
OK   Content.Server/Body/Systems/BloodstreamSystem.cs
OK   Content.Server/_Mono/Traits/Physical/HemophiliaSystem.cs
OK   Content.Server/Medical/Components/HealingComponent.cs
OK   Content.Server/Medical/HealingSystem.cs
```

`WolfmedOrganComponent.cs` field list read directly: `Health`, `MaxHealth`, `DestructionWound`, `DestructionWoundSeverity` — matches PLAN WP6 #7 exactly, registers as `WolfmedOrgan`, no collision found by grep. `_WF` style honoured: no license header, `/// <summary>` one-liners.

Decisions the WP6 section cites — D13, D14, D15, D8, D12, D26, D31 — all verified honoured by direct code inspection above (server-only relocation, `HealingComponent` kept + `HashSet<>` fields, `CirculatoryStreamSystem` trimmed to the four named members, organ health on `WolfmedOrganComponent`, `WolfmedDamageableSystem` facade used instead of `DamageableSystem`, `AmputationSystem` dependency+call both disabled, `ResolveHealingPartEvent` construction-site conversion only).

The report's 9 documented deviations (§3) were spot-checked against the diffs above and are accurate:
1. `WoundSystem`/`WoundFractureSystem` `HandlePartDamageApplied` made `public` — confirmed necessary (D13 moves the sole caller to a different assembly, no `InternalsVisibleTo`); PLAN §5.2's "no separate subscription" invariant untouched (checked in §4 above).
2. `WoundBleedingSystem` keeps both `using`s rather than swapping — confirmed correct (file needs `SharedBodySystem` from shared and `BloodstreamSystem` from server).
3. Healing part resolved from `TargetingComponent`, not a new `RequestedPart` field — confirmed `HealingDoAfterEvent.cs` is untouched and not in PLAN §3's hook list; `ResolveWoundTargetPart` correctly gated on `SharedTargetingSystem.IsSelectable` (HOOK 5).
4. `IsWoundDamaged` kept separate from `HasDamage` — confirmed `HasDamage`/`IsPartDamaged` byte-unchanged.
5. `DestroyOrgan` uses `RemoveOrgan` instead of a slot walk — confirmed `TryGetOrganInSlot`/`TryRemoveOrgan` do not exist in WG (only in the Onyx source).
6-9. Confirmed by direct inspection above (organ-health query pairing, circulatory fallback removal, `OrganFunctionChangedEvent` placement, `WolfmedOrganComponent`'s path/name).

No file listed in the WP6 table is missing; no PLAN decision cited by WP6 is violated.

---

## 7. Snapshot

```
git -C WG diff HEAD -- Content.Shared Content.Server Content.Client Resources Docs Content.IntegrationTests > C:/tmp/wolfmed-plan/snapshots/WP6.patch
git -C WG ls-files --others --exclude-standard -- Content.Shared Content.Server Content.Client Resources Docs Content.IntegrationTests > C:/tmp/wolfmed-plan/snapshots/WP6.untracked.txt
```

Written: `C:/tmp/wolfmed-plan/snapshots/WP6.patch` (477 lines), `C:/tmp/wolfmed-plan/snapshots/WP6.untracked.txt` (67 lines, new/moved files from this and earlier WPs since nothing is committed).

---

## Summary

| Check | Result |
|---|---|
| Build (Server) | PASS — 0 errors |
| Build (Client) | PASS — 0 errors |
| Upstream discipline | PASS — all 8 modified upstream files fully marked and each hook traced to an authorised PLAN §3 row for this or an earlier WP |
| Vendoring fidelity | PASS — all 7 vendored/moved `_Onyx` files' drift from Onyx is fully `// WOLFGATE`-marked; 1 verbatim file confirmed byte-identical |
| Subscriptions | PASS — 12 new pairs, all unique against the rest of WG, all matching PLAN §5.2 |
| Manifest | PASS — all touched files have rows, deviations section present, re-sync line bumped |
| Plan conformance | PASS — all 12 table files exist at planned destinations; all cited decisions honoured |
| Snapshot | Written |

**pass = true.** No blocker or major issues found.
